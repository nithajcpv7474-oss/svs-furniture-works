import prisma from '../src/config/prisma.js'; async function main() { try { const prodCount = await prisma.productionJob.count(); console.log('CHECK 1: ProductionJob count:', prodCount); } catch (e) { console.log('CHECK 1 ERROR:', e.message); } try { const orders = await prisma.order.findMany({ select: { id: true, orderNumber: true, orderStatus: true } }); console.log('CHECK 2: Orders:', orders); } catch (e) { console.log('CHECK 2 ERROR:', e.message); } } main().catch(console.error).finally(() => prisma.$disconnect());
